new p5((p) => {

    p.setup = function () {
        let canvas = p.createCanvas(700, 300);
        canvas.parent("duvaritenadam");
    };

    p.draw = function () {
        p.background(255);
        
    };
});
